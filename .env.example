# Morocco Healthcare Optimization — Environment Variable Template
# Copy this file to .env and fill in your values.
# NEVER commit .env to version control.

# ── LLM API (triage bot) ─────────────────────────────────────────────────────
# Only needed for Phase 5 LLM features. Local fallback works without this.
ANTHROPIC_API_KEY=your_anthropic_api_key_here
# OPENAI_API_KEY=your_openai_api_key_here   # alternative

# ── OSRM routing (optional, Phase 2 road-network distances) ──────────────────
# Leave blank to use straight-line KD-tree fallback (default).
OSRM_SERVER_URL=http://router.project-osrm.org

# ── Data paths (override defaults if needed) ─────────────────────────────────
# DATA_DIR=data/processed
# RAW_DATA_DIR=data/raw

# ── Logging ──────────────────────────────────────────────────────────────────
LOG_LEVEL=INFO
